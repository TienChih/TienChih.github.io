<?xml version='1.0' encoding='UTF-8'?>
<knowl mode="exercise" xmlns="https://spatext.clontz.org" version="0.2">
    
        <!-- {{#tasks}} -->
        <knowl>
            <!--{{#fraction}}-->
            <!-- {{#converges}} -->
                <content>
                    <p>
                        Use the Limit Comparison Test to determine if <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \frac{ {{top}} }{ {{bottom}} } </m> converges.
                    </p>
                </content>
                <outtro>
                    <p>
                        Let <m>\renewcommand{\log}{\ln} \displaystyle a_n = \frac{ {{top}} }{ {{bottom}} }, b_n = \frac{1}{n^{ {{P}} } }</m>. Note that <m>a_n</m> is a positive, eventually  decreasing sequence.  
                        We have that:

                            <me>\renewcommand{\log}{\ln} \displaystyle  \lim_{n\to\infty}  \frac{ {{top}} }{ {{bottom}} } \cdot \frac{ n^{ {{P}} } }{1} = \lim_{n\to\infty} \sqrt{\frac{ {{topsquare}} }{ {{bottomsquare}} } \cdot \frac{n^{ {{Psquare}} }}{1} } = \sqrt{\lim_{n\to\infty}\frac{ {{final}} }{ {{bottomsquare}} } } = {{simpcoeff}}.</me>
                        We can see that <m>0 &lt; {{simpcoeff}} &lt; \infty </m> so by the Limit Comparison Test, <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \frac{ {{top}} }{ {{bottom}} } </m> converges if and only if <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \frac{ 1 }{ n^{ {{P}} } } </m> converges, which it does by the <m>p</m>-test since <m>{{P}} &gt; 1</m>.  So  <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \frac{ {{top}} }{ {{bottom}} } </m> converges.
                    </p>
                </outtro>
            <!-- {{/converges}} -->
            <!-- {{#diverges}} -->
                <content>
                    <p>
                        Use the Limit Comparison Test to determine if <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \frac{ {{top}} }{ {{bottom}} } </m> converges.
                    </p>
                </content>
                <outtro>
                    <p>
                        Let <m>\renewcommand{\log}{\ln} \displaystyle a_n = \frac{ {{top}} }{ {{bottom}} }, b_n = \frac{1}{n^{ {{P}} } }</m>. Note that <m>a_n</m> is a positive, eventually  decreasing sequence.  
                        We have that:

                            <me>\renewcommand{\log}{\ln} \displaystyle  \lim_{n\to\infty}  \frac{ {{top}} }{ {{bottom}} } \cdot \frac{ n^{ {{P}} } }{1} = \lim_{n\to\infty} \sqrt{\frac{ {{topsquare}} }{ {{bottomsquare}} } \cdot \frac{n^{ {{Psquare}} }}{1} } = \sqrt{\lim_{n\to\infty}\frac{ {{final}} }{ {{bottomsquare}} } } = {{simpcoeff}}.</me>
                        We can see that <m>0 &lt; {{simpcoeff}} &lt; \infty </m> so by the Limit Comparison Test, <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \frac{ {{top}} }{ {{bottom}} } </m> converges if and only if <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \frac{ 1 }{ n^{ {{P}} } } </m> converges, which it does not by the <m>p</m>-test since <m>{{P}} \leq 1</m>.  So  <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \frac{ {{top}} }{ {{bottom}} } </m> diverges.
                    </p>
                </outtro>
            <!-- {{/diverges}} -->
            <!--{{/fraction}}-->

            <!--{{#trans}}-->
            <!-- {{#converges}} -->
                <content>
                    <p>
                        Use the Limit Comparison Test to determine if <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \left( {{Fn}} \right) </m> converges.
                    </p>
                </content>
                <outtro>
                    <p>
                        Let <m>\renewcommand{\log}{\ln} \displaystyle a_n = {{Fn}} , b_n = \frac{1}{n^{ {{P}} } }</m>. Note that <m>a_n</m> is a positive, eventually  decreasing sequence.  
                        We have that:

                            <me>\renewcommand{\log}{\ln} \displaystyle  \lim_{n\to\infty}  \frac{ {{Fn}} }{ \frac{1}{ n^{ {{P}} } } } = \lim_{n\to\infty} \frac{ {{Fd}} }{ {{Pd}} } = {{limit}}.</me>
                        (Not all work shown.)  We can see that <m>\renewcommand{\log}{\ln} 0 &lt; {{limit}} &lt; \infty </m> so by the Limit Comparison Test, <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \left( {{Fn}} \right) </m> converges if and only if <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \frac{ 1 }{ n^{ {{P}} } } </m> converges, which it does by the <m>p</m>-test since <m>{{P}} &gt; 1</m>.  So  <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty\left( {{Fn}} \right)</m> converges.
                    </p>
                </outtro>
            <!-- {{/converges}} -->
            <!-- {{#diverges}} -->
                <content>
                    <p>
                        Use the Limit Comparison Test to determine if <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \left( {{Fn}} \right) </m> converges.
                    </p>
                </content>
                <outtro>
                    <p>
                        Let <m>\renewcommand{\log}{\ln} \displaystyle a_n = {{Fn}} , b_n = \frac{1}{n^{ {{P}} } }</m>. Note that <m>a_n</m> is a positive, eventually  decreasing sequence.  
                        We have that:

                            <me>\renewcommand{\log}{\ln} \displaystyle  \lim_{n\to\infty}  \frac{ {{Fn}} }{ \frac{1}{ n^{ {{P}} } } } = \lim_{n\to\infty} \frac{ {{Fd}} }{ {{Pd}} } = {{limit}}.</me>
                        (Not all work shown.)  We can see that <m>\renewcommand{\log}{\ln} 0 &lt; {{limit}} &lt; \infty </m> so by the Limit Comparison Test, <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \left(  {{Fn}} \right) </m> converges if and only if <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \frac{ 1 }{ n^{ {{P}} } } </m> converges, which it does not by the <m>p</m>-test since <m>{{P}} \leq 1</m>.  So  <m>\renewcommand{\log}{\ln} \displaystyle \sum_{ n= {{k0}} }^\infty \left(  {{Fn}} \right) </m> diverges.
                    </p>
                </outtro>
            <!-- {{/diverges}} -->
            <!--{{/trans}}-->

            
        </knowl>
<!-- {{/tasks}} -->
</knowl>
